# Known credential identifiers the agent is expected to use. Access to a credential not listed
# here is flagged NEW (Tier B). Use least-privilege scopes (read-only where possible).
gmail_readonly
# gmail_send          # enable only when send workflows are approved
# service_api_limited # limited-role service account: no billing, no admin, no user management
